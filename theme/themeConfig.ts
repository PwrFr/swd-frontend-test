// theme/themeConfig.ts
import type { ThemeConfig } from "antd";

const theme: ThemeConfig = {
  token: {
    fontSize: 16,
    colorPrimary: "#FFA200",
    // colorError: "#",
    colorBgBase: "#fff",
  },
};

export default theme;
